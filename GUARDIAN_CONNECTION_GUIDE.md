# 보호자 연결 안내

1. 틀니 사용자 계정으로 로그인하고 설정의 **가족 초대코드**를 확인합니다. 복사 버튼을 눌러 보호자에게 직접 전달합니다.
2. 보호자는 자신의 휴대폰 첫 화면에서 **가족 초대코드가 있어요**를 누릅니다. 처음이면 **보호자 회원가입 후 연결하기**, 기존 계정이 있으면 **보호자 계정으로 로그인하기**를 선택합니다. 초대코드는 로그인 후 연결 화면에서 입력합니다. 두 앱은 같은 서버 프로젝트에 연결되어 있어야 합니다.
3. 6자리 코드를 입력하고 틀니 사용자가 나의 누구인지 선택한 뒤 연결합니다. 공백과 소문자는 자동 정리됩니다.
4. 연결 완료 화면에서 **가족 현황 보기**를 누릅니다. 사용자 이름, 관리·검진 기록과 도움 요청을 확인할 수 있습니다.
5. 양쪽 설정의 **계정과 가족 연결**에서 목록을 새로고침하거나 연결을 해제할 수 있습니다. 해제하면 사용자 초대코드가 바뀌므로 재연결할 때 최신 코드를 확인합니다.

## 설치 및 서버 전제

리뷰 브랜치 `codex/ios-device-readiness`를 반영하고 `npm ci`, `npm run ios:run`으로 Xcode에서 다시 설치합니다. 기존 서버 마이그레이션 `004_account_safety.sql`의 초대 연결 RPC와 접근 정책이 필요합니다. 이번 보호자 화면 수정은 새 마이그레이션을 추가하지 않습니다. 운영 서버 적용 여부는 이 작업 환경에서 확인하지 않았습니다.

## 실제 두 계정으로 확인할 항목

- 사용자 코드로 보호자 계정 연결 및 양쪽 목록 표시
- 사용자 관리 완료 후 보호자 앱에서 갱신 확인
- 잘못된 코드 입력 후 재시도
- 연결 해제 후 보호자의 기록 접근 차단 및 최신 코드로 재연결

자동 UI 검사는 모의 서버를 사용합니다. 실제 서버와 두 휴대폰의 연결 성공을 대신 증명하지 않습니다. 보호자 앱이 닫힌 상태의 원격 푸시 알림은 현재 제공하지 않습니다. 음성 알림의 실제 기기 재생 확인도 별도입니다.
