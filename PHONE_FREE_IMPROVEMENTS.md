# 2026-09-23 휴대폰 없이 진행한 개선

이 문서는 `codex/ios-device-readiness` 검토 브랜치의 변경입니다. 자동 검사 결과는 PR #1의 최신 커밋 검사를 확인합니다. 이 브랜치 반영만으로 Mac 프로젝트·설치된 아이폰 앱·운영 Supabase가 자동 업데이트되지는 않습니다.

## 수정 내역

| 항목 | 이번 변경 | 확인 방법 |
|---|---|---|
| 보호자 연결 | 대기·승인·거절·취소·만료 구분, 정확한 유효 기한, 재실행 시 대기 상태 우선 표시, 중복 입력 차단 | 요청 → 재실행 → 취소/재요청 → 승인 |
| 첫 사용 | 본인 회원가입 버튼에서 바로 본인 가입, 보호자 경로를 별도 표시, 승인 후 공유 범위 안내 | 첫 화면 두 경로 확인 |
| 저장 실패 | 알림 시간의 명시적 저장과 실패 시 입력 보존, 오프라인 안내, 조회 실패와 빈 기록 구분 | 실패 후 입력 유지·재시도 확인 |
| 접근성 | 알림 시간 줄바꿈, 검진의 긴 치과명 줄바꿈, 큰 글자에 맞춘 날짜별 리포트, 도움말 키보드 초점 | 320/390px Chromium, 390px WebKit 화면 검사 |
| 리포트 | 중복·비활성·미래·오늘 기록 제외, 기록 없는 기간을 ‘놓침’이나 ‘첫 주’로 단정하지 않음, 집계 기준 표시 | 달력 경계·중복·빈 데이터 자동 검사 |
| 검진 | 방문일 수정·삭제 확인, 실패 시 날짜 보존, 중복 저장 방지, 본인 기록만 변경 | 새 SQL 적용 후 방문 기록 저장·수정·삭제 |
| 접근 권한 | 대기 재요청 시 만료 연장 방지, 재요청에 새 ID 발급, 오래된 승인 차단, 연결된 보호자의 새 요청 차단 | 실제 SQL의 승인 전/해제 후 접근 차단 검사 |
| 출시 준비 | 앱 내 도움말, 스토어 소개 초안, 출시 설정 확인 명령, 읽기 전용 DB 준비 상태 확인 SQL | `npm run release:check`, `db/check_readiness.sql` |

입력 보존은 현재 화면을 유지한 상태에서의 실패·재시도를 의미합니다. 오프라인 자동 업로드나 앱 종료 후 임시 입력 복구를 구현한 것은 아닙니다.

## 이번에 추가로 적용할 서버 파일

9월 16일 대화에서는 `004`, `005`, `006` 실행 성공 화면과 Anonymous Sign-Ins 설정 완료 보고를 받았습니다. 해당 설정을 다시 반복할 필요는 없습니다. 이 작업 환경에서 운영 서버를 직접 조회하거나 변경하지는 않았습니다.

이번 새 파일은 **`db/migrations/007_request_and_visit_integrity.sql` 하나**입니다.

1. 동일한 DentureCare Supabase 프로젝트의 SQL Editor에서 새 쿼리를 엽니다.
2. 검토한 `007_request_and_visit_integrity.sql` 파일의 **전체 내용**을 붙여넣고 실행합니다. 기존 `001`~`006`을 다시 실행하지 않습니다.
3. 성공 후 `db/check_readiness.sql` 전체를 새 쿼리에서 실행합니다. `visit_edit_ready`, `visit_delete_ready`가 `true`인지 확인합니다. 이 조회는 데이터 변경이 없습니다.
4. 이 검사는 함수 존재만 확인합니다. 실제 사용자/보호자 로그인과 승인·조회·해제는 별도 검수합니다.

`007`은 기존 기록을 지우지 않고 다시 실행할 수 있도록 작성했습니다. 기존 중복 검진 이력도 임의 삭제하지 않습니다. 새 앱의 검진 방문 저장·수정·삭제는 `007`이 있어야 동작합니다. 없으면 실패를 표시하며 성공으로 처리하지 않습니다. `007`만 적용해도 새 요청 규칙은 기존 앱의 RPC에도 적용됩니다.

## Mac의 최신 코드 적용

Finder의 실제 `Denture Care program` 폴더에서 터미널을 열고 먼저 다음을 실행합니다.

```bash
git status --short
git branch --show-current
```

첫 명령에 파일 목록이 있으면 현재 수정 내용을 보존한 뒤 병합해야 합니다. 목록이 없으면 다음을 실행합니다.

```bash
git fetch origin
git switch codex/ios-device-readiness
git pull --ff-only origin codex/ios-device-readiness
npm ci
npm run verify
```

충돌·오류가 나면 그 단계에서 멈추고 메시지를 확인합니다. 아이폰 설치가 필요해졌을 때 `npm run ios:run`을 실행하고 Xcode에서 실제 아이폰을 선택합니다. `Any iOS Device`는 실행 대상이 아닙니다.

## 출시 준비

```bash
npm run release:check
```

이 명령은 `.env`의 Supabase 공개 설정, 개인정보처리방침 주소, 문의 이메일과 Capacitor 설정을 검사합니다. 비밀 값은 출력하지 않습니다. 형식 통과가 실제 메일 수신·URL 공개 접근·법률 검토·스토어 심사 통과를 뜻하지는 않습니다. 운영자의 실제 정보는 [개인정보 작성 워크시트](PRIVACY_POLICY_WORKSHEET.md)에서 확정합니다. 소개 문구는 [스토어 소개 초안](STORE_LISTING_DRAFT.md)에 준비했습니다.

## 여전히 별도 검수가 필요한 항목

- 사용자의 Xcode/iPhone 환경에서 발생한 메모리 종료: 이번 화면 개선으로 해결됐다고 판단하지 않습니다. 기존 [메모리 진단 안내](IOS_MEMORY_TROUBLESHOOTING.md)를 사용합니다.
- 음성 알림: 잠금 화면에서 실제 한국어 음성이 나는지 미확인입니다. 기본 알림음 수신 확인과 구분합니다.
- 보호자 연결: 자동 검사는 가짜 API 또는 로컬 PostgreSQL입니다. 실제 Supabase와 양쪽 휴대폰의 승인·해제 검수는 남아 있습니다.
- 보호자 백그라운드 푸시·자동 치과 예약·초대 링크로 앱 설치: 구현된 기능이 아닙니다.
- 운영자 정보·지원 메일 수신·개인정보처리방침 공개와 검토, 실제 인증 메일 발송, Apple 서명·TestFlight·App Store 제출: 별도 준비와 최종 검수 대상입니다.
